// initial state
const initialState = {};

// Action types

// Reducer
const reducer = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

// Action creators

// Selectors

// exports
export default reducer;
